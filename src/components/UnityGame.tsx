import React, { useEffect, useRef } from "react";

export const UnityGame = ({ initialBalance }: UnityGameProps) => {
  return (
    <div>
      Unity Game Component {initialBalance} {}
    </div>
  );
};
